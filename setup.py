from setuptools import setup, find_packages

setup(
    name="phys305_hw2",  # Package name
    version="0.0.0",  # Version number
    packages=find_packages(),  # Automatically find sub-packages
    install_requires=[],  # Add dependencies if needed
)